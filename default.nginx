server {
        listen 80 default_server;
        listen [::]:80 default_server;

        root /var/www/concepts.com;

        index index.html;

        server_name hrcpt.online;

        location / {
                try_files $uri $uri/ =404;
        }
}

